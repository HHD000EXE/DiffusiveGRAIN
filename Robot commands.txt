First, connect to the same network that the Raspberry-Pi is connected to:


Then, SSH to the Raspberry-Pi:


ssh sandbot@192.168.1.12   #password is: 123

Then, move to appropriate directory:


cd ros2_testing/src/py_pubsub/py_pubsub

Then, run the python script:


sudo python3 next_step.py

Below is the abbreviation sheet of all the commands:
Note: it goes like: command, integer. The integer indicates how many times to play the command. Even if the command is only going to be run once, it has to be indicated like (RF, 1) instead of (RF)


Example:


RF,1  (run right forward 1 time)
RF,2  (run right forward 2 times)
LF,1  (run left forward 1 time)
LR: left rear
RR: right rear
BOND: bond gair
LEFT: left turn
RIGHT: right turn
